{
  "name": "albi",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0"
  },
  "dependencies": {
    "@nestjs/axios": "^4.0.1",
    "axios": "^1.16.1"
  }
}