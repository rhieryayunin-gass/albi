export function logInfo(
  text: string
) {
  console.log(
    `[INFO] ${text}`
  );
}

export function logWarning(
  text: string
) {
  console.log(
    `[WARNING] ${text}`
  );
}

export function logError(
  text: string
) {
  console.log(
    `[ERROR] ${text}`
  );
}